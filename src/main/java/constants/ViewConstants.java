package constants;

/**
 * This class contain the various constants that we use in the view.
 */
public final class ViewConstants {

    /**DIVISOR used in GameView for the resolution of elements. */
    public static final double DIVISOR_1_5 = 1.5;

    /**DIVISOR used in GameView for the resolution of elements. */
    public static final int DIVISOR_10 = 10;

    /**DIVISOR used in GameView for the resolution of elements. */
    public static final int DIVISOR_15 = 15;

    /**DIVISOR used in GameView for the resolution. */
    public static final int DIVISOR_20 = 20;

    /**DIVISOR used in GameView for the resolution of elements. */
    public static final int DIVISOR_25 = 25;

    /**DIVISOR used in GameView for the resolution of elements. */
    public static final int DIVISOR_30 = 30;

    /**Number of LINES in the battlefield grid. */
    public static final int GRID_LINES = 5;

    /**Number of COLUMNS in the battlefield grid. */
    public static final int GRID_COLUMNS = 14;

    private ViewConstants() { }
}
